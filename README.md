# AD8232 EMG — ESP32 + OTA

Single-channel surface EMG from the forearm using an AD8232 module and ESP32.
Data streams over WiFi to a Python GUI. The ESP32 runs on a USB powerbank;
flashing is done wirelessly via OTA after the first USB flash.

---

## Wiring — AD8232 → ESP32

```
AD8232        ESP32
─────────     ──────────────
3.3V    ───── 3.3V
GND     ───── GND
OUTPUT  ───── GPIO 34   (ADC1_CH6 — analog EMG signal)
LO+     ───── GPIO 16   (lead-off detection +)
LO-     ───── GPIO 17   (lead-off detection -)
SDN     ───── 3.3V      (always on)
```

---

## Electrode Placement (forearm, surface EMG)

```
Palm facing UP — inner forearm

  Elbow                                        Wrist
    │                                            │
────┤█████████████████████████████████████───────┤────
    │                                            │
    │   [YELLOW LA+]    [RED RA-]    [GREEN RL]  │
    │        │               │            │      │
    │    ~5 cm from      ~3 cm toward   wrist    │
    │    elbow           wrist          bone     │
    │    (muscle belly)                (bony,    │
    │                                  no muscle)│
```

| Cable  | AD8232 Pin | Position |
|--------|-----------|----------|
| YELLOW | LA+       | Muscle belly, ~5 cm from elbow (inner forearm) |
| RED    | RA−       | 2–3 cm toward wrist from yellow, same line |
| GREEN  | RL (REF)  | Wrist bone (ulnar styloid) — no muscle tissue |

> Both signal electrodes (YELLOW + RED) must be on the **inner forearm** (palm up),
> aligned **along** the muscle fiber direction.
> Clean skin with alcohol before attaching for better contact.

---

## Setup

### 1. Credentials
```bash
cp .env.example .env
# edit .env — fill in WIFI_SSID, WIFI_PASSWORD, OTA_PASSWORD
```

### 2. First flash (USB required once)
```bash
./flash.sh
```

### 3. All subsequent flashes (OTA — powerbank only)
```bash
./flash.sh
```

`flash.sh` auto-detects: ESP32 reachable via WiFi → OTA, otherwise → USB fallback.

Override OTA host:
```bash
OTA_HOST=192.168.1.42 ./flash.sh
```

---

## Running

```bash
# terminal 1 — API server (connects to ESP32, exposes REST)
python3 emg_api.py

# terminal 2 — monitor + auto-launch GUI
./monitor.sh
```

Or launch GUI standalone (requires API server running):
```bash
python3 gui/emg_gui.py
```

---

## Calibration (in GUI)

1. Press **CALIBRATE**
2. Relax arm completely → press **START** → hold still for 5 s
3. Tense arm / make a fist → press **START** → hold for 5 s
4. Threshold is set automatically and saved to `calibration.json`
5. Calibration loads automatically on next start — no need to redo it

---

## Data Stream

The ESP32 sends CSV over TCP on port `8888`:
```
raw,rms
2105,42.3
2089,38.7
LEAD_OFF        ← electrodes detached
```

- `raw` — 12-bit ADC value (0–4095, center ≈ 2048)
- `rms` — RMS envelope over 200 ms window

---

## Project Structure

```
AD8232-EMG/
├── firmware/
│   └── emg_ota/
│       ├── emg_ota.ino     ESP32 firmware (WiFi + OTA + EMG sampling)
│       ├── config.h        pin definitions, sampling config
│       └── secrets.h       generated by flash.sh — do not commit
├── gui/
│   └── emg_gui.py          live GUI with built-in calibration wizard
├── emg_api.py              HTTP REST API server (stdlib only, no pip)
├── monitor.sh              terminal monitor + auto-starts GUI
├── flash.sh                compile + OTA/USB upload
├── .env                    WiFi credentials (gitignored)
├── .env.example            credential template
├── calibration.json        saved calibration (gitignored)
├── API.md                  REST API reference for AI agents
└── README.md
```

---

## Ports

| Service          | Port |
|-----------------|------|
| EMG REST API    | 5555 |
| ESP32 TCP stream | 8888 |
| ESP32 OTA       | 3232 |
